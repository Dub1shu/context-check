# トークン節約Tips

## 効果の大きい順

| 優先度 | 行動 | ねらい |
|---|---|---|
| 1 | 無関係なタスクの間で `/clear` | 古い会話やファイルを次の全リクエストから外す |
| 2 | `/context` と `/usage` で現状を確認 | 感覚ではなく、消費源を特定する |
| 3 | 指示を具体化し、完了条件を先に渡す | 探索、確認質問、手戻りを減らす |
| 4 | `CLAUDE.md`、MCP、スキルを見直す | 毎回または起動時に入る固定コストを減らす |
| 5 | モデルと effort を仕事の難しさに合わせる | 過剰な推論コストを避ける |
| 6 | 大量調査だけをサブエージェントへ分離 | メイン会話へ大量の読み取り結果を残さない |

## 1. まず可視化する

### `/usage` で利用量を見る

`/usage`（`/cost` はエイリアス）で、現在のセッションのトークン利用状況を確認できる。API利用時に表示される金額はローカル計算の推定値で、正式な請求額は Claude Console が基準になる。Pro／Max などのサブスクリプションでは、セッション金額よりプランの利用状況の方が意味を持つ。

見るタイミング:

- 作業開始前と終了後
- 想定より長引いたデバッグの後
- モデル、MCP、サブエージェントの使い方を変えた前後

### `/context` で「何が入っているか」を見る

コンテキストに入るのは会話だけではない。読み込んだファイル、コマンド出力、`CLAUDE.md`、auto memory、スキル、MCPツールも含まれる。`/context` で内訳を見ると、想定外に大きい項目が見つかることが多い。

### ステータスラインで常時表示する

`/statusline show model name and context percentage with a progress bar` のように依頼すると、コンテキスト使用率を常時表示できる。ステータスライン自体はローカルで動作し、APIトークンを消費しない。

## 2. セッションをタスク単位で区切る

### 無関係な作業へ移るなら `/clear`

古い文脈は、その後のメッセージのたびに処理対象になる。バグ修正から資料作成へ移るなど、前の会話が不要になった時点で `/clear` する。先に `/rename` しておけば、後から `/resume` で戻れる。

判断は単純で、

- 前のやり取りを次の作業で使わない → `/clear`
- 同じ作業を続けるが会話が重い → `/compact <残す内容>`
- 会話の本筋に残す必要がない小さな質問 → `/btw`

### `/compact` には残す内容を書く

```text
/compact 認証バグの原因、変更済みファイル、未実行のテスト、次の作業だけを残す
```

自動圧縮に任せるより、何を保持するか明示できる。ただし `CLAUDE.md` など起動時の固定コンテキストは圧縮後も再読み込みされるため、肥大化した固定情報は圧縮では解決しない。

### 修正ループが続いたら、学びを取り込んでやり直す

同じ問題で何度も（目安として3回以上）訂正しているなら、失敗した試行もコンテキストを占有している。原因、対象ファイル、制約、正解例を整理し、新しいセッションで再開した方が速いことが多い。

## 3. 最初の依頼を具体的にする

短い依頼が常に安いわけではない。曖昧な一言で探索と修正が増えるより、最初に必要な情報を渡す方が総消費は小さくなる。

依頼に入れる5点:

1. **目的**: 何を実現するか
2. **範囲**: 対象ファイル、画面、機能
3. **制約**: 変更禁止箇所、利用可能なライブラリ、互換性
4. **参考**: 既存の似た実装、仕様、エラー箇所
5. **完了条件**: 必要なテスト、出力形式、期待結果

```text
src/auth/session.ts のログアウト時の例外を修正してください。
公開APIは変更せず、既存の Result 型を使ってください。
src/auth/token.ts のエラー処理を参考にし、未ログイン時と期限切れのテストを追加してください。
対象テストと型チェックが通れば完了です。
```

ログや資料を貼るときは、関係する範囲だけに絞る。巨大なログなら「末尾200行」「ERROR を含む前後」「失敗したテスト名」など、先に切り出しておく。

## 4. 毎回入る固定コンテキストを小さくする

### `CLAUDE.md` は「常に必要なこと」だけ

公式ドキュメントは、具体的・簡潔・構造化された記述と、1ファイル200行未満を目安としている。残すのは次のもの。

- よく使うビルド、テスト、lint コマンド
- プロジェクト固有の構成と重要な制約
- 常に守るコーディング規約
- Claude が繰り返し間違えた重要事項

特定作業だけで使う長い手順はオンデマンドのスキルへ、特定パスだけの規約は `.claude/rules/` の path-scoped rule へ移す。`CLAUDE.md` から別ファイルを import しても起動時に読み込まれる点は変わらないため、単に分割してもトークンは減らない。

### auto memory を定期的に掃除する

`/memory` で内容を確認し、古い前提や重複を削除する。`MEMORY.md` を索引として短く保ち、詳細をトピック別ファイルへ置けば、必要なときだけ読ませられる。

### 使っていないMCPを無効化する

MCPツールは遅延ロードされるが、ツール名などの情報はコンテキストに入る。`/mcp` と `/context` で確認し、今の作業で使わないサーバーは無効化する。同じ仕事を `gh`、`aws`、`gcloud` などのCLIで安全に実行できるなら、公式ガイドはCLIの方がコンテキスト効率がよいとしている。

### スキルを増やしすぎない

スキル本文は呼び出し時に読み込まれるが、説明は起動時のコンテキストに入る。用途が重なるスキル、長すぎる説明、使われていないスキルは整理する。

## 5. モデルと思考量を使い分ける

### 普段は必要十分なモデルを選ぶ

公式のコストガイドは、一般的なコーディングには Sonnet、複雑な設計判断や多段の推論には Opus という使い分けを示している。モデル名や既定値は変わるため、「常に特定モデル」ではなく仕事の難しさで選ぶ。

- 定型修正、局所的な実装、単純な調査 → 軽量な選択肢
- 複数領域にまたがる設計、難しい原因分析 → 高性能な選択肢
- 設計だけ高性能、実装は効率重視 → 利用環境で使えるなら `opusplan` も選択肢

### effort を上げっぱなしにしない

`/effort` で推論量を調整できる。低い effort は単純で範囲の狭い仕事、中程度はコストを重視する通常作業、高い effort は難しい推論に向く。`max` は無制限に深く考えさせる用途で、効果が頭打ちになる場合もあるため常用せず試してから使う。

拡張思考のトークンは、画面上で折りたたまれていても課金対象になる。単純作業では effort を下げるか、必要に応じて thinking を無効にする。品質が落ちて再試行が増えるなら戻す。

## 6. サブエージェントを正しく使う

サブエージェントは別コンテキストで大量のコード探索を行い、要約だけをメインへ返せるため、メイン会話を軽く保てる。向いているのは自己完結した調査。

- 認証処理の実装箇所を調べ、要点と関連ファイルだけ返す
- 大量のログを分析し、再現条件と有力な原因だけ返す
- 実装後に独立した観点でレビューする

一方、サブエージェント自体もトークンを消費する。短い変更、頻繁な往復が必要な作業、同じ文脈を共有する連続工程では、メイン会話の方が効率がよい。多数を同時に起動したり、詳細な全文を返させたりすると逆効果になる。

エージェントチームは各メンバーが独立したコンテキストを持つため、消費はおおむね人数に比例する。小さいチーム、焦点を絞った依頼、完了後の速やかな終了が基本になる。

## 7. 出力と探索を小さく保つ

- 巨大ファイルを丸ごと読ませず、検索して関連箇所を特定する
- コマンド出力は必要な行へ絞り、成功ログを延々と返さない
- 「まず調査だけ」「実装して対象テストだけ」のように作業範囲を区切る
- 期待する回答形式を「結論、根拠、次の操作」のように指定する
- 生成した長文を会話に貼り直さず、ファイルパスと変更点を参照する
- 全文説明ではなく、差分と判断理由を求める

ただし、出力を切り詰めすぎて重要なエラーを隠さないこと。最初は絞り込み、必要な場合だけ範囲を広げる。

## 8. うまくいかない「節約」

- **依頼を極端に短くする**: 曖昧さが探索と修正を増やす
- **圧縮だけに頼る**: 固定コンテキストは再読み込みされ、要約で細部も失われる
- **常に最軽量モデルを使う**: 難しい問題で失敗と再試行が増えれば逆効果
- **何でもサブエージェントへ渡す**: 起動ごとに別コンテキストが必要になる
- **検証を省く**: 不具合の持ち越しは大きな手戻りになる
- **利用上限とコンテキスト使用率を混同する**: 関連はするが、同じ指標ではない

## 9. 効果を確かめる

### 変える前に記録しておく

何かを変える前の1〜2週間、普段の使い方のまま次を記録しておくと、後で比べられる。

- 利用上限に達した回数
- 代表的なセッションの `/usage`
- 1タスクあたりの修正往復回数
- 完了までの時間、テスト結果、レビューでの差し戻し
- 使ったモデル、effort、サブエージェントの数

API を使っているなら Claude Console に正式な利用量とコストがある。OpenTelemetry を有効にすれば `claude_code.token.usage` と `claude_code.cost.usage` も取れる。`/usage` の推定金額と請求データは別物なので混同しない。

### トークンだけを見ない

| 観点 | 見るもの | 注意点 |
|---|---|---|
| 消費 | 週次トークン、コスト、上限到達回数 | 契約形態とモデル差を分ける |
| 成果 | 完了タスク、受理された変更、PR | 行数を生産性とみなさない |
| 品質 | テスト失敗、差し戻し、不具合 | 節約による品質低下を検知する |
| 手戻り | 同じ指示の訂正回数、再セッション数 | 曖昧な依頼や過剰な文脈の兆候 |
| 固定費 | `CLAUDE.md` 行数、MCP・スキル数 | 数だけでなく実際の `/context` も見る |

トークンが減っても手戻りや完了時間が増えているなら、その変更は失敗と判断してよい。

### 定期的に見直す

- `CLAUDE.md` は200行未満を目安に読み返す
- 追加したMCPやスキルは、まだ使っているかを確認して整理する
- モデル名、effort、既定値、コマンドは変わるため、公式ドキュメントに合わせて内容を更新する

## 10分でできること

1. `/usage` と `/context` を開いて現状を記録する
2. ステータスラインへコンテキスト使用率を表示する
3. 使っていないMCPを無効化する
4. `CLAUDE.md` から一時的な手順、重複、古い記述を外す
5. 通常作業のモデルと effort を見直す
6. 次のタスクから「目的・範囲・制約・参考・完了条件」を1回目の依頼に書く

根拠にした資料は [調査ソース](sources.md) にまとめてある。
